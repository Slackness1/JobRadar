# REJECTED — 试过但没保留的方案

> 防止重复试错。每条记:**试了什么 / 为什么放弃 / 证据 / 留下了什么**。
> 新条目追加在最上方,按日期倒序。代码若保留(默认关),也在这里写明,别再去"激活"。

---

## 2026-06-07 · quality_label「便宜模型投票 + 级联升级」放量 — 验证后否决

**试了什么**:给金融岗"进不进推荐池"的质量判别搭一套级联——便宜模型(deepseek-flash)配
"每公司一行"知识库背景跑全量,自一致性投票,只把先验硬规则命中或投票分歧的岗升级给强模型。
目标是在已经很便宜的判别上再省一道。完整 TDD 实现(8 任务,见
`docs/superpowers/plans/2026-06-07-enrich-cascade-cost-quality.md`),代码全部默认关。

**为什么放弃**(正式验证 50/200 样本,结果在 `data/_phase_g/{cascade_validation,divergence_map}.json`):
1. **省钱大头已被中转拿走,级联增量太小**。公开判别早已切到 0.05 折 GPT 中转,单次判别便宜到
   近乎可忽略。级联比"全部走中转强模型"在整个重点公司池(~5000 岗)也就差**约 1 块钱**,却带来
   实打实的质量波动——不划算。
2. **没过放量门槛**:与库里标签一致率 **69%**(门槛 90%)、自动升级率 **44%**(过高)、
   估算省钱 **24%**(门槛 50%)。升级率高是因为便宜模型在金融岗上自一致性差,投票常分歧。
3. **先验硬规则瞄错靶**:校准显示"零售/渠道销售"便宜模型其实 94% 一致(分歧仅 6%),全部强制
   升级是白花钱;反倒普通岗分歧率(24%)更高。规则放过了该查的、查了不用查的。
4. **参照不干净**:库里基线标签是 DeepSeek 强模型打的,级联升级调的是中转 GPT 强模型,两者本就
   不一致(中转偏宽爱判 good)。69% 里有一部分是两个强模型之争,不全是便宜模型的错——但也救不回门槛。

**留下了什么**:
- 级联代码(`app/services/phase_g/quality_cascade/` + 两个开关)**保留但默认关**,不要再去激活放量。
- **「公司背景注入」(`company_kb.py` + v3 prompt 注入,`QUALITY_KB_INJECTION_ENABLED`)是独立的、
  对强模型本身也可能有用的部分**,没被否决——若要评估,单独验"给强模型喂公司背景能否提升金融判别
  准确率",别和级联绑一起。
- 校准/验证脚本(`26_divergence_map.py` / `27_validate_cascade.py`)可复用于将来任何"便宜 vs 强"对比。
