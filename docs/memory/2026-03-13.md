# 2026-03-13

## 安全审查 skills 安装与验证
- 在 `project-a` workspace 安装了 4 个安全相关 skills：
  - `openclaw-skill-vetter` 1.0.0
  - `skill-scanner` 0.1.2
  - `clawsec` 1.0.0
  - `security-auditor` 1.0.0
- 登录 ClawHub 后，下载限流问题得到缓解，安装成功
- Gateway 刷新后，`project-a` agent 已能识别这些本地 skills

## 测试结论
### openclaw-skill-vetter
- 可调用
- 对 `skill-scanner` 的审查结论为低风险，可安装

### skill-scanner
- 脚本可直接运行
- 会对文档中的危险示例产生误报，不能单独作为拒绝依据

### security-auditor
- 可调用
- 已成功识别测试文件中的 SQL 注入、命令注入、硬编码 secret、缺失鉴权

### clawsec
- 初始安装内容不完整（ClawHub 包只带 `skill.md`）
- 已从其公开 GitHub 仓库补齐 `clawsec-monitor.py`、`run_tests.py`、`Dockerfile.clawsec`、`docker-compose.clawsec.yml`、`requirements.clawsec.txt`
- `python3 -m py_compile` 已通过，结构上已完整
- 但上游包在 ClawHub inspect 中标记为 `suspicious`，后续若要实际启用，只能做受控验证，不能默认信任
- 第二阶段受控验证已执行：`python3 run_tests.py` 结果 `28/28 PASS`
- 已对本地 skills 做集中审查并输出 `SECURITY_SKILL_AUDIT_2026-03-13.md`
- 当前结论：
  - `clawsec` → `ALLOW_WITH_CAUTION`
  - `openclaw-skill-vetter` → `ALLOW`
  - `security-auditor` → `ALLOW`
  - `skill-scanner` → `ALLOW_WITH_CAUTION`

## 新增策略
- 新建 `SECURITY_SKILL_REVIEW.md`
- 将第三方 skill 安装 / 更新 / 试用前置安全审查写入 `AGENTS.md`、`SOUL.md`、`TOOLS.md`
- 后续在 `project-a` 中处理第三方 skill 时，默认遵循该流程

## 运行环境与用户上下文
- 用户明确要求记住：当前 `project-a` / OpenClaw 运行在**腾讯云轻量应用服务器**上，通过**飞书**与用户沟通；用户平时的工作场景在**Windows 工作电脑**上，后续需求默认从这个上下文出发理解
- `AGENTS.md` 已补充“飞书触发识别”规则：包含 `运行 JobRadar 爬取`、`启动/停止/查看 JobRadar 状态` 等关键词的飞书消息，视为项目 A 优先工作指令

## JobRadar 服务与运维状态
- 当日晚些时候确认：JobRadar 前后端最初**没有运行**；原因不是项目损坏，而是服务器上**原先没有 Docker / docker compose 可用**，且也没有其他进程管理器托管服务
- 已临时用本地进程方式拉起过 JobRadar：
  - 后端：`../venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000`
  - 前端：`npm run dev -- --host 0.0.0.0 --port 5173`
- 已验证当时：后端 `GET /api/health` 返回 `{"status":"ok"}`，前端首页可访问
- 已保存一张前端截图到：`/home/ubuntu/.openclaw/workspace-projecta/data/screenshots/jobradar-home.png`

## OpenClaw / 基础能力状态
- 已安装并启用 Docker：
  - `docker.io` 已安装
  - `docker-compose-v2` 已安装
  - Docker daemon 处于 `active`
- 当前会话对 `/var/run/docker.sock` 的普通组权限**未立即刷新**；`sudo docker ...` 可用，普通 `docker ...` 可能需新登录会话后才直接可用
- OpenClaw 内建 browser 自动化已恢复：
  - `browser.enabled=true`
  - `executablePath=/usr/bin/google-chrome`
  - `headless=true`
  - `noSandbox=true`
  - 后续状态显示 `running=true`、`cdpReady=true`
- 服务器本机 `google-chrome` 可用，也可用 headless 模式直接截图
- OpenClaw 的 `web_fetch` 可用；内建 `web_search` 仍因缺少 Brave API key 不可用（用户曾提供 Tavily key，但未接入，也未写入长期记忆/代码）

## Docker 化 JobRadar 的已知阻塞
- 已创建可复用脚本：
  - `scripts/jobradar-docker-up.sh`
  - `scripts/jobradar-docker-down.sh`
  - `scripts/jobradar-docker-status.sh`
- 还未把 JobRadar 完整切换到 Docker 常驻运行；当前主要阻塞点是 **backend Docker build 失败**
- 已定位到一个关键坑：曾给 Docker daemon / compose 传入宿主机 loopback 代理 `127.0.0.1:7890`，容器内该地址指向容器自身，导致代理不可达；随后已撤回 compose 中相关代理注入
- 最新失败点表现为 backend Dockerfile 在容器内 `apt-get update/install` 阶段无法正确获取 Debian 包索引，导致 `libnss3` 等依赖解析失败；前端镜像拉取已成功，backend 镜像构建仍需进一步修复后，才能继续完成“改用 Docker + systemd 保活”
