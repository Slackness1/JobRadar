# 2026-03-18 Durable Memory

## 用户口径与优先级（重要）
- 用户明确要求：第三档（T3）互联网公司以其提供的“校招官网入口”为准；其他未开春招的先不优先。
- 用户后续确认：光大银行、邮储银行、建信金科“当前未开”，应在配置中标记暂缓抓取。
- 用户对复杂爬取任务偏好：允许/鼓励通过 `sessions_spawn` 调用子代理（glm-4.7-flashx）执行，主代理负责计划与审阅。

## T3 校招抓取修复与结果（阶段性）
- 已按用户提供官网入口补齐/修正多个站点映射与 targets（含哈啰、盒马、莉莉丝、Soul、虎扑、搜狐、新浪、爱奇艺、贝壳、同程等）。
- 关键跑通结果（最小页验证阶段）曾出现：
  - 360(100)、哈啰(9)、盒马(10)、莉莉丝(43)、金山办公(1)、Soul(37)
  - 虎扑(14)、搜狐(1)、新浪(2)
- 后续修复后四家问题站点（爱奇艺/微博/贝壳/同程）已可出数（示例轮次：爱奇艺28、微博2、贝壳15、同程10）。

## 银行爬取修复（阶段性）
- 银行名单按用户给定梯队推进，先做“Unknown site 消除 + 映射补齐 + 专用/兜底抓取器”。
- 已新增/调整多家银行抓取函数与 SITE_MAP（含中行专项、交行/建行等入口适配）。
- 关键状态：
  - 中国银行：用户提供新入口后已跑出约34条（去重后可能新增0）。
  - 上海农商银行：用户提供 `shrcb.zhiye.com/campus/jobs` 后可出数（示例2条）。
  - 建设银行：用户提供 `job2.ccb.com` 入口后可出数（示例12条）。
  - 光大/邮储/建信金科：按用户口径标记“未开，暂缓抓取”。
- 银行数据已做一次 CSV -> 主库同步，结果示例：scanned=5505, inserted=238, total_jobs=22560。

## 岗位情报（job_intel / media crawler）现状结论
- 当前为 MVP：后端路由、任务编排、前端 intel 组件已存在。
- 真实抓取主要在 xiaohongshu 最小链路；其他平台（maimai/nowcoder/boss/zhihu）多为 stub/模板或兜底 mock。
- `job_intel` 与独立 `xiaohongshu_login_and_crawl.py` 仍属两套链路，尚未完全统一成生产级多平台流水线。

## 协作工作流共识
- 关于 worktree：用于“同仓并行”；A/B 两项目仍应各自仓库内独立分支与合并，不跨仓混合。
