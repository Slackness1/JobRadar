# 2026-03-27

## Durable notes
- 用户明确要求：银行批量爬取任务要先从飞书多维表格取入口，再写入队列，由 `main` 调度自动跑。
- 本次指定入口表：`26届实习&秋招&春招汇总表1`（app_token=`QybYb9aQna9XeBsTf6xcPFnRngk`，table_id=`tblGAgR5CzuNZjZv`）。
- 经验沉淀：处理该表时不能只看首批返回结果，需在全量记录范围内按公司名做匹配检索（用户明确指出“全量有8000+条记录”）。
- 已执行动作：将匹配到且可用的银行入口任务写入 `data/bank_queue.json`，供 `main` 后续调度。
